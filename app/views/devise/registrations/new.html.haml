.container
  .col-md-6.col-md-offset-1
    %h2 Criar uma nova conta no DonaMaria
    %p
      Você está a um passo de começar a contribuir para a diminuição das desigualdades de gênero entre os representantes dos cidadãos brasileiros! É só preencher os campos abaixo e confirmar a sua nova conta. Mas se já tiver uma,
      = link_to 'clique aqui', new_session_path(resource_name)
      para visitar a página de início de sessão.
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = devise_error_messages!
      .form-group
        = f.label :email, 'E-mail'
        = f.email_field :email, autofocus: true, placeholder: 'seu.email@exemplo.com', class: 'form-control'
      .form-group
        = f.label :password, 'Senha'
        = f.password_field :password, autocomplete: 'off', class: 'form-control'
        %p.help-block
          No mínimo 8 dígitos
      .form-group
        = f.label :password_confirmation, 'Confirmação de senha'
        = f.password_field :password_confirmation, autocomplete: 'off', class: 'form-control'
        %p.help-block
          Repita a senha anterior
      .pull-right
        = f.submit "Cadastrar", class: 'btn btn-primary'
      .link-wrapper
        .pull-left
          = link_to "Acesse sua conta aqui", new_session_path(resource_name)
