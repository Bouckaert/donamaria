<div class='container'>
  <div class="row infobox">
      <div class=' col-md-4'>
        <!-- <%=@patrimonio_masculino%> --> <label>Patrimonio Masculino</label> <b>R$ 1.918.003.634,98</b>
        <!-- <%=@patrimonio_feminino%> --> <label>Patrimonio Feminino</label> <b>R$ 172.719.217,91</b>
      </div>
      <div class=' col-md-4'>
        <!--<%=@receita_masculino%> --> <label>Receita Masculino</label> <b>R$ 124.690.971,72</b>
        <!--<%=@receita_feminino%> --> <label>Receita Feminino</label> <b>R$ 15.372.761,6</b>
      </div>
      <div class=' col-md-4'>
        <!--<%=@despesa_masculino%> --> <label>Despesa Masculino</label> <b>R$ 124.518.658,34</b>
        <!--<%=@despesa_feminino%> --> <label>Despesa Feminino</label> <b>R$ 14.467.072,91</b>
      </div>
  </div>
</div>
<%= @representatives.all.count%>
<div class='container'>
  <% @representatives.each do |representative| %>
    <div class="row box">
      <div class=' col-md-4 representative'>
        <div class='row'>
          <div class= 'col-md-6'>
            <b><%= representative.name %></b>
            <!-- BUG::: <b><%#= representative.candidatures.first.candidate_number %></b> -->
            <% representative.candidatures.each do |candidature| %>
              <b><%= candidature.candidate_number %></b>
            <%end%>

<%= link_to (image_tag("politica.png",size: "100x100")),representative_path(representative.id)%>

<!--            <%= link_to (image_tag("politica.png",size: "100x100")), representatives_path(representative.type, representative) %> -->



<!--            representative_path(representative.id)%> -->


            <label><%= representative.occupation %> <%= representative.gender %></label>
            <div class="dropdown">
              <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu<%= "#{(representative.id)}" %>" data-toggle="dropdown" aria-expanded="true">Ano Eleitoral
                <span class="caret"></span>
              </button>
              <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu<%= "#{(representative.id)}" %>">
                <% representative.candidatures.each do |candidature| %>
                  <li role="presentation"><%= link_to candidature.election_year, "#AnoEleição-#{(candidature.id)}", id: candidature.id, :class => "btn" %></li>
                <%end%>
              </ul>
            </div>
          </div>
          <% representative.candidatures.each do |candidature| %>
            <div id='<%= "candidature-#{(candidature.id)}" %>' class= 'col-md-6 hide candidature'>
              <%= render partial: "candidature", locals: {candidature: candidature} %>
            </div>
          <%end%>
        </div>
      </div>
      <div class='col-md-7 canvas-proposal'>
        <% representative.proposals.each do |proposal| %>
            <%= render partial: "proposal", locals: {proposal: proposal} %>
        <% end %>
      </div>
    </div>
  <%end%>
</div>


<%= paginate @representatives %>

<td><%= link_to "See all #{representative.type.pluralize}", sti_representative_path(representative.type) %></td>

